import { createApp } from 'vue'
import App from './App'
createApp App
  .mount '#app'

# warning: this is a coffeescript but with .js extension only.
# warning: coffee is not allowed use in script setup.
